(()=>{
'use strict';
const VERSION='3.0.0',TYPES=['group','air','sea','road','power','bank','mobility'];
const clone=v=>globalThis.structuredClone?structuredClone(v):JSON.parse(JSON.stringify(v));
const num=v=>Math.max(0,Number(v)||0), now=s=>Number(s.simSeconds)||0;
const COLLECTION_PROFILES=Object.freeze({
 group:{channel:'تحويل عميل تعاقدي',source:'عملاء المجموعة',service:'خدمات المجموعة'},
 air:{channel:'تسوية حجوزات وتذاكر وشحن جوي',source:'مركز تحصيل الطيران · وكلاء الحجز والعملاء',service:'تذاكر منفذة وشحن جوي وحجوزات شركات'},
 sea:{channel:'تحويلات فواتير شحن وبوالص بحرية',source:'مركز تحصيل الشحن البحري · الشاحنون ووكلاء الشحن',service:'حاويات وشحنات وعقود نقل بحري'},
 road:{channel:'تحصيل فواتير نقل بعد التسليم',source:'مركز تحصيل اللوجستيات · عملاء النقل والتوزيع',service:'أوامر نقل وتسليم وعقود توزيع'},
 power:{channel:'تسوية فواتير توريد الطاقة',source:'مركز تحصيل الطاقة · مشترو الطاقة والعملاء',service:'طاقة موردة وعقود شراء طاقة'},
 bank:{channel:'تحصيل فوائد ورسوم مصرفية',source:'مركز تحصيل البنك · المقترضون وعملاء الخدمات',service:'فوائد ورسوم خدمات؛ لا تشمل أصل القروض أو الودائع'},
 mobility:{channel:'تسوية نقاط البيع وبوابة الدفع',source:'مركز تحصيل التنقل · مدفوعات الركاب والعملاء',service:'رحلات مكتملة وحسابات تنقل الشركات'}
});
function collectionProfile(company){return COLLECTION_PROFILES[company]||COLLECTION_PROFILES.group;}
function recordCollection(s,{company,amount,reference,invoiceNumbers=[],counterparty='',channel='',grossAmount=amount,deductions=0,sourceRefs=[],purposeCategory='تحصيل إيراد',purposeDetail='',accountBalanceBefore=null,accountBalanceAfter=null}){
 const profile=collectionProfile(company),account=book(s,company).accounts[0],sender=partyMeta(s,counterparty||profile.source,'customer'),detail=String(purposeDetail||profile.service),row={id:reference,reference,at:now(s),company,beneficiaryCompany:company,from:sender.name||counterparty||profile.source,fromPartyId:sender.id,to:account.id,toAccount:account.id,amount,grossAmount,deductions,netAmount:amount,invoiceNumbers:[...invoiceNumbers],sourceRefs:sourceRefs.slice(0,80).map(String),channel:channel||profile.channel,service:profile.service,purposeCategory:String(purposeCategory||'تحصيل إيراد'),purposeDetail:detail,paymentMethod:'تحويل/تسوية مصرفية',kind:'revenue-collection',collection:true,label:'حوالة تحصيل إيراد',status:'منفذة',note:`تحصيل ${invoiceNumbers.join(' · ')} · ${detail}`};
 if(Number.isFinite(Number(accountBalanceBefore)))row.accountBalanceBefore=Number(accountBalanceBefore);if(Number.isFinite(Number(accountBalanceAfter)))row.accountBalanceAfter=Number(accountBalanceAfter);
 s.finance.transfers.unshift(row);ledger(s,company,row);recordWorldFinance(s,{partyId:sender.id,counterparty:row.from,company,direction:'incoming',amount,reference,transferRef:reference,documentNumber:invoiceNumbers[0]||null,note:row.note,settled:true});return row;
}
function companyName(s,t){return t==='group'?(s.profile?.name||'المجموعة'):(s.companyRegistry?.[t]?.legalName||({air:'الطيران',sea:'الشحن البحري',road:'النقل البري',power:'الطاقة',bank:'البنك',mobility:'التنقل الذكي'}[t]||t));}
function partyMeta(s,name,role='counterparty'){const W=globalThis.GH_BUSINESS_WORLD,id=W?.partyIdForName?.(s,name,role)||null,party=id?W?.resolveParty?.(s,id):null;return {id,name:party?.legalName||party?.displayName||String(name||'')};}
function recordWorldFinance(s,p){try{return globalThis.GH_BUSINESS_WORLD?.execute?.({state:s},'record-finance',p)||null;}catch(error){console.warn('business-world finance bridge rejected',error);return null;}}
function makeBook(s,t,balance=0){const c=t==='group'?'GH':String(t).toUpperCase();return {type:t,accounts:[{id:`${c}-OPER-001`,name:'الحساب الجاري',currency:'USD',balance:num(balance)},{id:`${c}-RES-002`,name:'حساب الاحتياطي',currency:'USD',balance:0}],ledger:[],taxPayable:0,taxPaid:0,vat:{output:0,input:0,creditCarry:0,periodOutputStart:0,periodInputStart:0},debt:0,lastReconciledAt:now(s)};}
function ensure(s){
 s.finance=s.finance&&typeof s.finance==='object'?s.finance:{};for(const k of ['invoices','payables','receivables','cheques','periods','journalEntries','transfers','payrollReports','dailyCompanyReports'])s.finance[k]=Array.isArray(s.finance[k])?s.finance[k]:[];
 s.finance.pendingDailyCash=s.finance.pendingDailyCash&&typeof s.finance.pendingDailyCash==='object'&&!Array.isArray(s.finance.pendingDailyCash)?s.finance.pendingDailyCash:{};for(const t of TYPES.filter(type=>type!=='group')){const pending=Number(s.finance.pendingDailyCash[t]);s.finance.pendingDailyCash[t]=Number.isFinite(pending)?pending:0;}
 s.finance.paymentSequence=Math.max(1,Math.floor(Number(s.finance.paymentSequence)||1));s.finance.invoiceSequence=Math.max(1,Math.floor(Number(s.finance.invoiceSequence)||1));
 if(!Number.isSafeInteger(Number(s.finance.journalSequence))||Number(s.finance.journalSequence)<0){const archived=s.finance.auditArchive?.records?.journalEntries,rows=[...s.finance.journalEntries,...(Array.isArray(archived)?archived:[])];s.finance.journalSequence=rows.reduce((max,row)=>{const match=/^JE-(\d+)$/.exec(String(row?.id||''));return match?Math.max(max,Number(match[1])||0):max;},0);}else s.finance.journalSequence=Math.floor(Number(s.finance.journalSequence));
 s.treasury=s.treasury&&typeof s.treasury==='object'?s.treasury:{accounts:[],ledger:[],paymentQueue:[]};s.treasury.accounts=Array.isArray(s.treasury.accounts)?s.treasury.accounts:[];s.treasury.ledger=Array.isArray(s.treasury.ledger)?s.treasury.ledger:[];
 while(s.treasury.accounts.length<3){const i=s.treasury.accounts.length;s.treasury.accounts.push({id:['GH-OPER-001','GH-RES-002','GH-INV-003'][i],name:['الحساب الجاري','حساب الاحتياطي','الاستثمارات'][i],currency:'USD',balance:0});}
 const had=s.companyFinance&&typeof s.companyFinance==='object'&&!Array.isArray(s.companyFinance)&&Object.keys(s.companyFinance).length>0;s.companyFinance=had?s.companyFinance:{};
 s.companyFinance.group=s.companyFinance.group||makeBook(s,'group');s.companyFinance.group.accounts=s.treasury.accounts;
 for(const t of TYPES){if(!s.companyFinance[t])s.companyFinance[t]=makeBook(s,t);const b=s.companyFinance[t];b.accounts=Array.isArray(b.accounts)&&b.accounts.length?b.accounts:makeBook(s,t).accounts;b.ledger=Array.isArray(b.ledger)?b.ledger:[];for(const a of b.accounts)a.balance=Number.isFinite(Number(a.balance))?Number(a.balance):0;b.taxPayable=Number.isFinite(Number(b.taxPayable))?Number(b.taxPayable):0;b.taxPaid=Number.isFinite(Number(b.taxPaid))?Number(b.taxPaid):0;b.debt=Number.isFinite(Number(b.debt))?Number(b.debt):0;b.vat=b.vat&&typeof b.vat==='object'?b.vat:{};for(const k of ['output','input','creditCarry','periodOutputStart','periodInputStart'])b.vat[k]=Number.isFinite(Number(b.vat[k]))?Number(b.vat[k]):0;}
 s.companyFinance.group.accounts=s.treasury.accounts;
 if(!had&&(s.openedCompanies||[]).length){const opened=s.openedCompanies.filter(t=>TYPES.includes(t)&&t!=='group'),src=s.treasury.accounts[0],pool=num(src.balance)*.55,weights=opened.map(t=>[t,Math.max(1,(s.assets||[]).filter(a=>a.type===t).length*2+1)]),sw=weights.reduce((n,x)=>n+x[1],0)||1;let alloc=0;weights.forEach(([t,x],i)=>{const a=i===weights.length-1?Math.max(0,pool-alloc):Math.floor(pool*x/sw);s.companyFinance[t].accounts[0].balance=a;alloc+=a;});src.balance=Math.max(0,num(src.balance)-alloc);}
 s.companyBudgets=s.companyBudgets&&typeof s.companyBudgets==='object'&&!Array.isArray(s.companyBudgets)?s.companyBudgets:{};backfillPayrollTransfers(s);reconcile(s);return s.companyFinance;
}
function backfillPayrollTransfers(s){
 for(const doc of s.finance.invoices){
  const payroll=doc?.budgetLine==='payroll'||doc?.payrollShortfall===true;if(!payroll)continue;
  const t=TYPES.includes(doc.company)?doc.company:'group',settled=['مدفوعة','مسددة','محصلة'].includes(doc.status),reportId=String(doc.payrollReportId||''),reference=String(doc.transferReference||`PAYTR-${reportId||doc.number}-${t}${settled?'':'-DUE'}`),existing=s.finance.transfers.find(row=>row.reference===reference||row.id===reference||row.documentNumber===doc.number&&['payroll-transfer','payroll-payable'].includes(row.kind));
  doc.transferReference=existing?.reference||reference;const payable=s.finance.payables.find(row=>row.number===doc.number);if(payable){payable.payrollShortfall=true;payable.payrollReportId=payable.payrollReportId||doc.payrollReportId||null;payable.transferReference=doc.transferReference;}
  if(existing)continue;const from=s.companyFinance[t]?.accounts?.[0]?.id||`${String(t).toUpperCase()}-OPER-001`;
  s.finance.transfers.unshift({id:reference,reference,at:Number(doc.at)||now(s),from,to:doc.counterparty||'حسابات الموظفين',amount:num(doc.total??doc.amount),note:doc.note||`مسير رواتب ${companyName(s,t)}`,company:t,kind:settled?'payroll-transfer':'payroll-payable',label:settled?'حوالة مسير رواتب':'حوالة رواتب مستحقة',status:settled?'منفذة':'مستحقة',documentNumber:doc.number,payrollReportId:doc.payrollReportId||null,migrated:true});
 }
}
function book(s,t='group'){const type=TYPES.includes(t)?t:'group',result=s?.companyFinance?.[type];if(!result||!Array.isArray(result.accounts)||!result.accounts.length)throw new Error(`finance-book-missing:${type}`);return result;}
function operating(s,t='group'){const value=Number(book(s,t).accounts?.[0]?.balance);return Number.isFinite(value)?value:NaN;} function total(s,t='group'){return (book(s,t).accounts||[]).reduce((n,a)=>{const value=Number(a.balance);return n+(Number.isFinite(value)?value:NaN);},0);}
function budgetView(s,t='group'){t=TYPES.includes(t)?t:'group';const period=Math.floor(Math.floor(now(s)/86400)/30),source=s?.companyBudgets?.[t],b=source&&typeof source==='object'?clone(source):{limit:0,spent:0,reserved:0,period,enabled:false,lines:{},spentByLine:{},reservedByLine:{}};for(const k of ['limit','spent','reserved'])b[k]=num(b[k]);b.enabled=!!b.enabled;b.lines=b.lines&&typeof b.lines==='object'?b.lines:{};b.spentByLine=b.spentByLine&&typeof b.spentByLine==='object'?b.spentByLine:{};b.reservedByLine=b.reservedByLine&&typeof b.reservedByLine==='object'?b.reservedByLine:{};for(const k of ['payroll','fuel','maintenance','marketing','insurance','technology','capex','other'])b.lines[k]=num(b.lines[k]);if(Number(b.period)!==period){b.period=period;b.spent=0;b.reserved=0;b.spentByLine={};b.reservedByLine={};}return b;}
function mutableBudget(s,t='group'){ensure(s);t=TYPES.includes(t)?t:'group';const view=budgetView(s,t);s.companyBudgets[t]=view;return view;}
const budget=budgetView;
function lineFor(note='',method=''){const t=`${note} ${method}`.toLowerCase();if(/راتب|رواتب|payroll/.test(t))return'payroll';if(/وقود|fuel/.test(t))return'fuel';if(/صيان|maintenance|mro/.test(t))return'maintenance';if(/تسويق|marketing/.test(t))return'marketing';if(/تأمين|insurance/.test(t))return'insurance';if(/تقنية|technology|software|نظام/.test(t))return'technology';if(/شراء|إنشاء|بناء|أصل|طائرة|سفينة|شاحن|capex/.test(t))return'capex';return'other';}
function remaining(s,t){const b=budget(s,t);return !b.enabled||b.limit<=0?Infinity:Math.max(0,b.limit-b.spent-b.reserved)} function lineRemaining(s,t,l){const b=budget(s,t),lim=num(b.lines[l]);return !b.enabled||lim<=0?Infinity:Math.max(0,lim-num(b.spentByLine[l])-num(b.reservedByLine[l]));}
function canSpend(s,t,a,l=null){a=Number(a);if(!Number.isFinite(a)||a<0)return false;if(s.godMoney&&s.infiniteMoney)return true;return operating(s,t)>=a&&remaining(s,t)>=a&&(!l||lineRemaining(s,t,l)>=a);}
// نفس فحص operating(s,t)>=a الخام + تجاوز الأموال اللانهائية فقط - بلا فحص الميزانية remaining()
// الإضافي الذي تفرضه canSpend، حتى لا يتغير سلوك هذه الدوال حين يكون وضع الإله مطفأً.
function hasFunds(s,t,a){return (s.godMoney&&s.infiniteMoney)||operating(s,t)>=Number(a);}
function coverInfiniteFunds(s,t,a,reason='تغطية وضع الأموال اللانهائية'){
 const requested=num(a),shortfall=Math.max(0,requested-operating(s,t));if(!(s.godMoney&&s.infiniteMoney)||shortfall<=0)return 0;
 const account=book(s,t).accounts[0],reference=`INF-${t}-${String((Number(s.finance?.journalSequence)||0)+1).padStart(8,'0')}`;account.balance+=shortfall;
 journal(s,t,reason,[{account:account.id,debit:shortfall},{account:'حقوق ملكية وضع اللعب',credit:shortfall}],reference);
 ledger(s,t,{at:now(s),from:'تمويل وضع اللعب',to:account.id,amount:shortfall,note:reason,company:t,kind:'sandbox-capital-cover',label:'تغطية وضع اللعب',fundingSource:'sandbox-unlimited-money',status:'منفذة',reference});return shortfall;
}
function consumeBudget(s,t,a,l){const b=mutableBudget(s,t);a=num(a);if(b.enabled&&b.limit>0)b.spent+=a;if(l)b.spentByLine[l]=num(b.spentByLine[l])+a;} function reserveBudget(s,t,a,l='other'){a=num(a);if(!canSpend(s,t,a,l))return false;const b=mutableBudget(s,t);if(b.enabled&&b.limit>0)b.reserved+=a;b.reservedByLine[l]=num(b.reservedByLine[l])+a;return true;} function consumeReserved(s,t,a,l='other'){const b=mutableBudget(s,t),held=Math.min(num(a),num(b.reservedByLine[l]));b.reserved=Math.max(0,b.reserved-held);b.reservedByLine[l]=Math.max(0,num(b.reservedByLine[l])-held);consumeBudget(s,t,a,l);} function releaseReserved(s,t,a,l='other'){const b=mutableBudget(s,t),held=Math.min(num(a),num(b.reservedByLine[l]));b.reserved=Math.max(0,b.reserved-held);b.reservedByLine[l]=Math.max(0,num(b.reservedByLine[l])-held);return held;}
function reconcile(s){if(!s.companyFinance)return Number(s.cash)||0;let cash=0,debt=0,taxPayable=0,taxPaid=0;for(const t of TYPES){const b=s.companyFinance[t];if(!b)continue;for(const account of b.accounts||[]){const value=Number(account.balance);if(!Number.isFinite(value)||value<0)throw new Error(`invalid-account-balance:${t}`);cash+=value;}for(const [key,target] of [['debt','debt'],['taxPayable','taxPayable'],['taxPaid','taxPaid']]){const value=Number(b[key]);if(!Number.isFinite(value)||value<0)throw new Error(`invalid-${key}:${t}`);if(target==='debt')debt+=value;else if(target==='taxPayable')taxPayable+=value;else taxPaid+=value;}}s.cash=cash;s.debt=debt;if(s.finance){s.finance.taxPayable=taxPayable;s.finance.taxPaid=taxPaid;}return s.cash;}
function ledger(s,t,e){book(s,t).ledger.unshift(e);s.treasury.ledger.unshift(e);s.treasury.ledger=s.treasury.ledger.slice(0,1200);}
function journal(s,t,desc,lines,ref=''){ensure(s);const rows=(lines||[]).map(x=>({account:String(x.account||''),debit:num(x.debit),credit:num(x.credit)})).filter(x=>x.account&&(x.debit>0||x.credit>0)),dr=rows.reduce((n,x)=>n+x.debit,0),cr=rows.reduce((n,x)=>n+x.credit,0);if(!rows.length||Math.abs(dr-cr)>.02)throw new Error(`قيد غير متوازن: ${desc}`);const sequence=++s.finance.journalSequence,row={id:`JE-${String(sequence).padStart(8,'0')}`,company:t,description:desc,lines:rows,sourceRef:ref,at:now(s)};s.finance.journalEntries.unshift(row);return row;}
function invoice(s,{kind,amount,note,method='تحويل بنكي',taxable=true,status='مدفوعة',company='group',counterparty='',number=null,dueDay=null,sourceRef=null,settlementAccount=null}={}){ensure(s);company=TYPES.includes(company)?company:'group';amount=num(amount);const tax=taxable?Math.round(amount*(.15/1.15)):0,subtotal=Math.max(0,amount-tax),doc=number||`${company==='group'?'GH':company.toUpperCase()}-${String(s.finance.invoiceSequence).padStart(6,'0')}`;if(!number)s.finance.invoiceSequence++;const rawCounterparty=String(counterparty||(kind==='دخل'?'عميل تعاقدي مسجل':'طرف تعاقدي مسجل')),cp=partyMeta(s,rawCounterparty,kind==='دخل'?'customer':'supplier'),row={number:doc,company,companyName:companyName(s,company),accountId:book(s,company).accounts[0].id,kind,amount,subtotal,tax,total:amount,note:String(note||''),method,status,counterparty:cp.name||rawCounterparty,counterpartyPartyId:cp.id,at:now(s)};if(dueDay!=null)row.dueDay=dueDay;if(sourceRef)row.sourceRef=String(sourceRef);if(settlementAccount)row.settlementAccount=String(settlementAccount);s.finance.invoices.unshift(row);recordWorldFinance(s,{partyId:cp.id,counterparty:row.counterparty,company,direction:kind==='دخل'?'incoming':'outgoing',amount,reference:sourceRef||doc,documentNumber:doc,note:row.note,settled:false});const settled=['مدفوعة','مسددة','محصلة'].includes(status);if(!settled)(kind==='دخل'?s.finance.receivables:s.finance.payables).unshift({...row,dueDay:row.dueDay??Math.floor(now(s)/86400)+30});const vat=book(s,company).vat;if(taxable){if(kind==='دخل')vat.output+=tax;else vat.input+=tax;}book(s,company).taxPayable=Math.max(0,(num(vat.output)-num(vat.periodOutputStart))-(num(vat.input)-num(vat.periodInputStart))-num(vat.creditCarry));const cash=book(s,company).accounts[0].id,counter=kind==='دخل'?'ذمم مدينة':'ذمم دائنة',settlement=settlementAccount?String(settlementAccount):cash;journal(s,company,`${doc} · ${note}`,kind==='دخل'?[{account:settled?settlement:counter,debit:amount},{account:'إيرادات تشغيلية',credit:subtotal},{account:'ضريبة مخرجات VAT',credit:tax}]:[{account:'مصروف تشغيلي',debit:subtotal},{account:'ضريبة مدخلات VAT',debit:tax},{account:settled?settlement:counter,credit:amount}],sourceRef||doc);return row;}
function transfer(s,p){ensure(s);const a=num(p.amount),from=String(p.from),to=String(p.to),reference=String(p.ref||p.reference||`TR-${Math.floor(now(s))}-${s.finance.transfers.length+1}`);if(from===to||!TYPES.includes(from)||!TYPES.includes(to)||a<=0)throw new Error('invalid-transfer');const prior=s.finance.transfers.find(row=>row.reference===reference||row.id===reference);if(prior){if(prior.fromCompany!==from||prior.toCompany!==to||Math.abs(num(prior.amount)-a)>.01)throw new Error('transfer-reference-conflict');return {transferred:true,status:'transferred',from,to,amount:a,reference,idempotent:true};}if(!hasFunds(s,from,a))throw new Error('insufficient-cash');coverInfiniteFunds(s,from,a,'تغطية تحويل داخلي في وضع اللعب');const fa=book(s,from).accounts[0],ta=book(s,to).accounts[0];fa.balance-=a;ta.balance+=a;const e={id:reference,reference,at:now(s),from:fa.id,to:ta.id,fromCompany:from,toCompany:to,amount:a,note:p.note||'تحويل داخلي',kind:'intercompany',label:'حوالة داخلية منفذة',status:'منفذة',company:from,beneficiaryCompany:to};s.finance.transfers.unshift({...e});ledger(s,from,e);book(s,to).ledger.unshift({...e});journal(s,from,e.note,[{account:'استثمار/ذمم بين شركات المجموعة',debit:a},{account:fa.id,credit:a}],reference);journal(s,to,e.note,[{account:ta.id,debit:a},{account:'رأس مال/ذمم بين شركات المجموعة',credit:a}],reference);reconcile(s);return {transferred:true,status:'transferred',from,to,amount:a,reference,idempotent:false};}
function settleIntercompanyInterest(s,p={}){
 ensure(s);const from=String(p.from||p.company||''),to=String(p.to||'bank'),amount=num(p.amount),reference=String(p.ref||p.reference||'').trim();
 if(!TYPES.includes(from)||from==='bank'||to!=='bank'||amount<=0)throw new Error('invalid-intercompany-interest');
 if(!reference)throw new Error('intercompany-interest-reference-required');
 const prior=s.finance.journalEntries.filter(row=>row.sourceRef===reference);
 if(prior.length)return {settled:true,from,to,amount:0,reference,idempotent:true,cashPostedRevenue:0};
 if(!hasFunds(s,from,amount))throw new Error('insufficient-interest-cash');
 const payer=book(s,from),receiver=book(s,to),note=String(p.note||`فائدة تسهيل ائتماني · ${reference}`),payerName=companyName(s,from),bankName=companyName(s,to);
 coverInfiniteFunds(s,from,amount,'تغطية تسوية فائدة في وضع اللعب');payer.accounts[0].balance-=amount;receiver.accounts[0].balance+=amount;
 const expense=invoice(s,{kind:'مصروف',amount,note,method:'تسوية فائدة داخلية',taxable:false,status:'مدفوعة',company:from,counterparty:bankName,sourceRef:reference});
 const income=invoice(s,{kind:'دخل',amount,note:`دخل ${note}`,method:'تسوية فائدة داخلية',taxable:false,status:'محصلة',company:to,counterparty:payerName,sourceRef:reference});
 const event={at:now(s),from:payer.accounts[0].id,to:receiver.accounts[0].id,amount,note,kind:'intercompany-interest',company:from,beneficiaryCompany:to,reference,expenseDocumentNumber:expense.number,incomeDocumentNumber:income.number};
 Object.assign(event,{id:reference,status:'منفذة',collection:true,channel:collectionProfile(to).channel,service:'تحصيل فائدة تسهيل ائتماني',grossAmount:amount,deductions:0,invoiceNumbers:[income.number],sourceRefs:[reference],fromCompany:from,toCompany:to});expense.transferReference=reference;income.transferReference=reference;s.finance.transfers.unshift({...event});ledger(s,from,event);receiver.ledger.unshift(event);reconcile(s);
 return {settled:true,from,to,amount,reference,expenseInvoiceNumber:expense.number,incomeInvoiceNumber:income.number,idempotent:false,cashPostedRevenue:amount};
}
function bulkTransfer(s,p){const rows=Array.isArray(p.rows)?p.rows:[],seen=new Set(),clean=[];for(const x of rows){const t=String(x.company||''),a=num(x.amount);if(!TYPES.includes(t)||t==='group'||a<=0)continue;if(seen.has(t))throw new Error('duplicate-beneficiary');seen.add(t);clean.push({company:t,amount:a});}if(!clean.length)throw new Error('empty-bulk-transfer');const total=clean.reduce((n,x)=>n+x.amount,0);if(!hasFunds(s,'group',total))throw new Error('insufficient-cash');const batch=p.batchId||`BULK-${Math.floor(now(s))}-${s.finance.transfers.length+1}`;for(const x of clean)transfer(s,{from:'group',to:x.company,amount:x.amount,note:`${p.note||'توزيع رأسمالي'} · ${batch}`,ref:`${batch}-${x.company}`});return {total,count:clean.length,batchId:batch};}
function transferReserve(s,p){const toReserve=p.toReserve!==undefined?!!p.toReserve:String(p.direction||'')!=='to-operating',b=book(s,p.company),src=toReserve?b.accounts[0]:b.accounts[1],dst=toReserve?b.accounts[1]:b.accounts[0],a=num(p.amount);if(a<=0)throw new Error('invalid-amount');if(!(s.godMoney&&s.infiniteMoney)&&num(src.balance)<a)throw new Error('insufficient-cash');if(s.godMoney&&s.infiniteMoney&&num(src.balance)<a){const shortfall=a-num(src.balance),reference=`INF-${p.company}-${String((Number(s.finance?.journalSequence)||0)+1).padStart(8,'0')}`;src.balance+=shortfall;journal(s,p.company,'تغطية احتياطي في وضع اللعب',[{account:src.id,debit:shortfall},{account:'حقوق ملكية وضع اللعب',credit:shortfall}],reference);}src.balance-=a;dst.balance+=a;const e={at:now(s),from:src.id,to:dst.id,amount:a,note:toReserve?'تحويل إلى الاحتياطي':'إعادة من الاحتياطي',company:p.company};ledger(s,p.company,e);journal(s,p.company,e.note,[{account:dst.id,debit:a},{account:src.id,credit:a}],p.ref||'');reconcile(s);return {amount:a,toReserve,from:src.id,to:dst.id};}
function spend(s,p){const t=p.company||'group',a=num(p.amount),line=p.line||lineFor(p.note,p.method);if(!canSpend(s,t,a,line))throw new Error('insufficient-funds-or-budget');const b=book(s,t);coverInfiniteFunds(s,t,a,'تغطية مصروف في وضع اللعب');b.accounts[0].balance-=a;consumeBudget(s,t,a,line);const doc=invoice(s,{kind:'مصروف',amount:a,note:p.note,method:p.method||'تحويل بنكي',taxable:p.taxable!==false,status:p.status||'مدفوعة',company:t,counterparty:p.counterparty||''});if((p.status||'مدفوعة')==='مدفوعة'){const e={at:now(s),from:b.accounts[0].id,to:doc.counterparty,toPartyId:doc.counterpartyPartyId||null,amount:a,note:p.note,company:t,kind:'cash-document',documentNumber:doc.number,reference:doc.number,status:'منفذة'};ledger(s,t,e);recordWorldFinance(s,{partyId:doc.counterpartyPartyId,counterparty:doc.counterparty,company:t,direction:'outgoing',amount:a,reference:doc.number,transferRef:doc.number,documentNumber:doc.number,note:p.note,settled:true});}reconcile(s);return doc;}
function payPayroll(s,p={}){
  const t=TYPES.includes(p.company)?p.company:'group',a=num(p.amount),b=book(s,t),reportId=String(p.reportId||''),transferId=String(p.reference||`PAYTR-${reportId||Math.floor(now(s)/86400)}-${t}`),prior=s.finance.transfers.find(row=>row.id===transferId||row.reference===transferId);
  if(prior){const priorDoc=s.finance.invoices.find(row=>row.number===prior.documentNumber);return priorDoc||{number:prior.documentNumber||transferId,company:t,amount:prior.amount,transferReference:transferId,idempotent:true};}
  if(a<=0)throw new Error('invalid-payroll-amount');if(!hasFunds(s,t,a))throw new Error('insufficient-payroll-cash');
  coverInfiniteFunds(s,t,a,'تغطية رواتب في وضع اللعب');b.accounts[0].balance-=a;consumeBudget(s,t,a,'payroll');
  const doc=invoice(s,{kind:'مصروف',amount:a,note:p.note||`مسير رواتب ${companyName(s,t)}`,method:'تحويل رواتب',taxable:false,status:'مدفوعة',company:t,counterparty:'حسابات الموظفين'});
  doc.payrollReportId=reportId||null;doc.budgetLine='payroll';doc.transferReference=transferId;
  const transfer={id:transferId,reference:transferId,at:now(s),from:b.accounts[0].id,to:'حسابات الموظفين',amount:a,note:doc.note,company:t,kind:'payroll-transfer',label:'حوالة مسير رواتب',status:'منفذة',documentNumber:doc.number,payrollReportId:doc.payrollReportId};s.finance.transfers.unshift({...transfer});ledger(s,t,transfer);reconcile(s);return doc;
}
function accruePayroll(s,p={}){
  const t=TYPES.includes(p.company)?p.company:'group',a=num(p.amount),number=String(p.number||`PAY-${t.toUpperCase()}-${Math.floor(now(s)/86400)}`),reportId=p.reportId||null;
  if(a<=0)throw new Error('invalid-payroll-amount');const existing=s.finance.invoices.find(row=>row.number===number);if(existing)return existing;
  const doc=invoice(s,{kind:'مصروف',amount:a,note:p.note||`رواتب مستحقة · ${companyName(s,t)}`,method:'تحويل رواتب',taxable:false,status:'مستحقة',company:t,counterparty:'حسابات الموظفين',number,dueDay:p.dueDay??Math.floor(now(s)/86400)});
  doc.budgetLine='payroll';doc.payrollShortfall=true;doc.payrollReportId=reportId;
  const payable=s.finance.payables.find(row=>row.number===number);if(payable){payable.budgetLine='payroll';payable.payrollShortfall=true;payable.payrollReportId=reportId;}
  const transferId=`PAYTR-${reportId||number}-${t}-DUE`,transfer={id:transferId,reference:transferId,at:now(s),from:book(s,t).accounts[0].id,to:'حسابات الموظفين',amount:a,note:doc.note,company:t,kind:'payroll-payable',label:'حوالة رواتب مستحقة',status:'مستحقة',documentNumber:number,payrollReportId:reportId};if(!s.finance.transfers.some(row=>row.id===transferId))s.finance.transfers.unshift({...transfer});doc.transferReference=transferId;ledger(s,t,{...transfer,from:'ذمم رواتب مستحقة',kind:'payroll-accrual'});return doc;
}
function recordPayrollReport(s,p={}){const report=clone(p.report||{}),id=String(report.id||'');if(!id||!Array.isArray(report.lines))throw new Error('invalid-payroll-report');const existing=s.finance.payrollReports.find(row=>row.id===id);if(existing)return existing;report.at=Number.isFinite(Number(report.at))?Number(report.at):now(s);report.total=report.lines.reduce((sum,line)=>sum+num(line.amount),0);report.paid=report.lines.reduce((sum,line)=>sum+num(line.paid),0);report.due=Math.max(0,report.total-report.paid);report.status=report.due>0?'مسجل مع رواتب مستحقة':'مصروف بالكامل';s.finance.payrollReports.unshift(report);s.finance.payrollReports=s.finance.payrollReports.slice(0,120);return report;}
function accrue(s,p){const t=p.company||'group',a=num(p.amount),line=p.line||lineFor(p.note,p.method||'قيد مستحق');if(!reserveBudget(s,t,a,line))throw new Error('insufficient-budget');const doc=invoice(s,{kind:'مصروف',amount:a,note:p.note,method:p.method||'قيد مستحق',taxable:p.taxable!==false,status:'مستحقة',company:t,counterparty:p.counterparty||'',dueDay:p.dueDay,number:p.number});doc.budgetLine=line;doc.budgetReserved=a;ledger(s,t,{at:now(s),from:'ذمم دائنة',to:doc.counterparty,amount:a,note:p.note,company:t,kind:'accrual-document',documentNumber:doc.number});return doc;}
function credit(s,p){
 const t=p.company||'group',a=Number(p.amount),profile=collectionProfile(t);if(!TYPES.includes(t)||!Number.isFinite(a)||a<=0)throw new Error('invalid-revenue-collection');
 if(p.sourceRefs!==undefined&&!Array.isArray(p.sourceRefs))throw new Error('invalid-collection-source-refs');
 const reference=String(p.reference||p.ref||`RCPT-${t}-${s.finance.invoiceSequence}`),prior=s.finance.transfers.find(row=>row.reference===reference);
 if(prior){if(prior.company!==t||Math.abs(prior.amount-a)>.01)throw new Error('collection-reference-conflict');return s.finance.invoices.find(row=>row.number===prior.invoiceNumbers?.[0])||{transferReference:reference,idempotent:true};}
 const day=Number(p.periodDay),hasDay=p.periodDay!==undefined&&Number.isSafeInteger(day)&&day>=0,cursors=s.finance.revenueCollectionDays||{};if(p.periodDay!==undefined&&!hasDay)throw new Error('invalid-collection-period');
 if(hasDay&&Number.isFinite(cursors[t])&&day<=cursors[t])return {transferReference:reference,idempotent:true};
 const doc=invoice(s,{kind:'دخل',amount:a,note:p.note||profile.service,method:profile.channel,taxable:p.taxable!==false,status:'مستحقة',company:t,counterparty:p.counterparty||profile.source,sourceRef:reference});
 collectReceivable(s,{number:doc.number,company:t,reference,channel:profile.channel,sourceRefs:p.sourceRefs||[]});
 if(hasDay){s.finance.revenueCollectionDays={...cursors,[t]:day};}return doc;
}
function collectReceivable(s,p){
 if(p.sourceRefs!==undefined&&!Array.isArray(p.sourceRefs))throw new Error('invalid-collection-source-refs');
 const n=String(p.number||''),item=s.finance.receivables.find(x=>x.number===n),inv=s.finance.invoices.find(x=>x.number===n);
 if(!item){if(inv?.status==='محصلة'&&inv.transferReference){if(p.company&&p.company!==inv.company)throw new Error('collection-company-mismatch');return {company:inv.company,amount:0,reference:inv.transferReference,idempotent:true};}throw new Error('receivable-not-found');}
 const t=item.company||'group',a=Number(item.total??item.amount),b=book(s,t),reference=String(p.reference||`RCPT-${t}-${n}`);
 if(!inv||inv.kind!=='دخل'||inv.company!==t||(p.company&&p.company!==t)||!Number.isFinite(a)||a<=0||Math.abs(Number(inv.total)-a)>.01)throw new Error('invalid-receivable-collection');
 if(s.finance.transfers.some(row=>row.reference===reference))throw new Error('collection-reference-conflict');
 const balanceBefore=num(b.accounts[0].balance);b.accounts[0].balance+=a;const balanceAfter=num(b.accounts[0].balance);inv.status='محصلة';inv.settledAt=now(s);inv.transferReference=reference;s.finance.receivables=s.finance.receivables.filter(x=>x.number!==n);
 journal(s,t,`تحصيل ${n} · ${collectionProfile(t).channel}`,[{account:b.accounts[0].id,debit:a},{account:'ذمم مدينة',credit:a}],reference);
 recordCollection(s,{company:t,amount:a,reference,invoiceNumbers:[n],counterparty:inv.counterparty,channel:p.channel,sourceRefs:p.sourceRefs||[],purposeCategory:'تحصيل فاتورة/إيراد',purposeDetail:inv.note||collectionProfile(t).service,accountBalanceBefore:balanceBefore,accountBalanceAfter:balanceAfter});reconcile(s);return {company:t,amount:a,reference,idempotent:false,balanceBefore,balanceAfter};
}
function settlePayable(s,p){
  const n=String(p.number||''),item=s.finance.payables.find(x=>x.number===n);if(!item)throw new Error('payable-not-found');
  const t=item.company||'group',a=num(item.total??item.amount),inv=s.finance.invoices.find(x=>x.number===n),line=inv?.budgetLine||item.budgetLine||lineFor(inv?.note||item.note||'',inv?.method||item.method||''),reserved=num(inv?.budgetReserved),payroll=inv?.payrollShortfall===true||item.payrollShortfall===true,linkedReport=s.finance.payrollReports.find(row=>row.id===(inv?.payrollReportId||item.payrollReportId)||row.lines?.some(reportLine=>reportLine.dueRef===n)),reportId=linkedReport?.id||null,b=book(s,t),method=String(p.method||'transfer');
  if(method==='cheque'){
    if(payroll)throw new Error('payroll-cheque-not-supported');
    const beneficiary=inv?.counterparty||item.counterparty||'طرف تعاقدي مسجل',cheque=issueCheque(s,{company:t,amount:a,beneficiary,invoiceNumber:n,note:inv?.note||item.note||`سداد ${n}`,dueDay:p.dueDay??item.dueDay,requestRef:p.requestRef||`PAYABLE-CHQ-${n}`,draweeBank:p.draweeBank,issuePlace:p.issuePlace,paymentPlace:p.paymentPlace,authorizedSignatory:p.authorizedSignatory});
    return {company:t,amount:a,method:'cheque',status:'issued',chequeId:cheque.id,reference:cheque.id,idempotent:cheque.idempotent===true};
  }
  if(method!=='transfer')throw new Error('invalid-payable-settlement-method');
  if(!hasFunds(s,t,a))throw new Error('insufficient-cash');if(!payroll&&!reserved&&!canSpend(s,t,a,line))throw new Error('insufficient-budget');
  coverInfiniteFunds(s,t,a,'تغطية ذمم مستحقة في وضع اللعب');const balanceBefore=num(b.accounts[0].balance);b.accounts[0].balance-=a;const balanceAfter=num(b.accounts[0].balance);if(reserved)consumeReserved(s,t,a,line);else consumeBudget(s,t,a,line);
  if(inv){inv.status='مسددة';inv.settledAt=now(s);inv.budgetReserved=0;inv.paymentMethod='تحويل بنكي';}s.finance.payables=s.finance.payables.filter(x=>x.number!==n);
  if(payroll&&linkedReport){const reportLine=linkedReport.lines?.find(row=>row.dueRef===n||(row.company===t&&num(row.due)>0));if(reportLine){reportLine.paid=num(reportLine.paid)+a;reportLine.due=Math.max(0,num(reportLine.amount)-num(reportLine.paid));reportLine.settledAt=now(s);linkedReport.paid=linkedReport.lines.reduce((sum,row)=>sum+num(row.paid),0);linkedReport.total=linkedReport.lines.reduce((sum,row)=>sum+num(row.amount),0);linkedReport.due=Math.max(0,linkedReport.total-linkedReport.paid);linkedReport.status=linkedReport.due>0?'مسجل مع رواتب مستحقة':'مصروف بالكامل';if(linkedReport.due===0)linkedReport.settledAt=now(s);}}
  const beneficiary=inv?.counterparty||item.counterparty||'طرف تعاقدي مسجل',payee=partyMeta(s,beneficiary,payroll?'employee':'supplier'),purpose=inv?.note||item.note||`سداد ${n}`,note=`${payroll?'صرف رواتب مستحقة':'سداد'} ${n} · ${purpose}`;journal(s,t,note,[{account:'ذمم دائنة',debit:a},{account:b.accounts[0].id,credit:a}],n);let reference='';
  if(payroll){const formal=s.finance.transfers.find(row=>row.documentNumber===n&&['payroll-payable','payroll-transfer'].includes(row.kind));reference=formal?.reference||`PAYTR-${reportId||n}-${t}-DUE`;if(formal){formal.kind='payroll-transfer';formal.label='حوالة رواتب مستحقة منفذة';formal.status='منفذة';formal.from=b.accounts[0].id;formal.settledAt=now(s);}else s.finance.transfers.unshift({id:reference,reference,at:now(s),from:b.accounts[0].id,to:payee.name||beneficiary,toPartyId:payee.id,amount:a,note,company:t,kind:'payroll-transfer',label:'حوالة رواتب مستحقة منفذة',status:'منفذة',documentNumber:n,payrollReportId:reportId});}
  const transferRef=reference||`PAY-${n}`,entry={id:transferRef,at:now(s),from:b.accounts[0].id,to:payee.name||beneficiary,toPartyId:payee.id,amount:a,note,purposeCategory:payroll?'رواتب':'سداد ذمة مورد',purposeDetail:purpose,paymentMethod:'تحويل بنكي',label:payroll?'حوالة رواتب مستحقة منفذة':'حوالة سداد ذمة',company:t,kind:payroll?'payroll-transfer':'payable-settlement',documentNumber:n,payrollReportId:reportId,reference:transferRef,status:'منفذة',accountBalanceBefore:balanceBefore,accountBalanceAfter:balanceAfter};
  if(!payroll)s.finance.transfers.unshift({...entry});ledger(s,t,entry);if(!payroll)recordWorldFinance(s,{partyId:payee.id,counterparty:entry.to,company:t,direction:'outgoing',amount:a,reference:transferRef,transferRef,documentNumber:n,note,settled:true});reconcile(s);return {company:t,amount:a,payroll,reportId,method:'transfer',reference:transferRef,balanceBefore,balanceAfter};
}
function payTaxes(s,p){const t=TYPES.includes(p.company)?p.company:'group',periods=s.finance.periods.filter(x=>(x.company||'group')===t&&x.status==='مستحق'),a=periods.reduce((n,x)=>n+num(x.amount),0),b=book(s,t);if(a<=0)return {company:t,amount:0,count:0};if(!hasFunds(s,t,a))throw new Error('insufficient-cash');coverInfiniteFunds(s,t,a,'تغطية ضريبة في وضع اللعب');b.accounts[0].balance-=a;journal(s,t,'سداد فترات VAT مستحقة',[{account:'ضريبة VAT مستحقة',debit:a},{account:b.accounts[0].id,credit:a}],`VAT-PAY-${Math.floor(now(s)/86400)}-${t}`);ledger(s,t,{at:now(s),from:b.accounts[0].id,to:'هيئة الزكاة والضريبة والجمارك',amount:a,note:'سداد فترات VAT مستحقة · تحويل حكومي',kind:'tax-settlement',company:t});b.taxPaid+=a;periods.forEach(x=>{x.status='مسدد';x.paidAt=now(s)});b.taxPayable=0;reconcile(s);return {company:t,amount:a,count:periods.length};}
function issueCheque(s,p){
  const t=TYPES.includes(p.company)?p.company:'group',a=num(p.amount),invoiceNumber=String(p.invoiceNumber||''),linked=invoiceNumber?s.finance.invoices.find(x=>x.number===invoiceNumber):null,payable=invoiceNumber?s.finance.payables.find(x=>x.number===invoiceNumber):null,requestRef=String(p.requestRef||'');
  if(invoiceNumber){if(!linked||!payable)throw new Error('cheque-linked-payable-not-found');if((linked.company||'group')!==t||(payable.company||'group')!==t)throw new Error('cheque-linked-company-mismatch');if(linked.kind!=='مصروف'||['مدفوعة','مسددة','محصلة'].includes(linked.status))throw new Error('cheque-linked-invoice-not-payable');if(Math.abs(num(linked.total??linked.amount)-a)>.01||Math.abs(num(payable.total??payable.amount)-a)>.01)throw new Error('cheque-linked-amount-mismatch');const beneficiary=String(p.beneficiary||'').trim(),counterparty=String(linked.counterparty||payable.counterparty||'').trim();if(beneficiary&&counterparty&&beneficiary!==counterparty)throw new Error('cheque-linked-beneficiary-mismatch');const existing=s.finance.cheques.find(row=>row.invoiceNumber===invoiceNumber&&row.status==='صادر');if(existing)return {...existing,idempotent:true};}
  if(requestRef){const prior=s.finance.cheques.find(row=>row.requestRef===requestRef);if(prior)return {...prior,idempotent:true};}
  if(a<=0)throw new Error('invalid-cheque-amount');const beneficiary=String(p.beneficiary||linked?.counterparty||payable?.counterparty||'').trim();if(!beneficiary)throw new Error('cheque-beneficiary-required');
  const payee=partyMeta(s,beneficiary,'supplier'),id=`${t==='group'?'GH':t.toUpperCase()}-CHQ-${String(s.finance.paymentSequence++).padStart(6,'0')}`,issuer=companyName(s,t),dueDay=Math.max(Math.floor(now(s)/86400),Math.floor(Number(p.dueDay) || linked?.dueDay || payable?.dueDay || Math.floor(now(s)/86400))),purpose=String(p.note||linked?.note||payable?.note||'سداد التزام تجاري');
  const r={id,chequeNumber:id,company:t,companyName:issuer,drawer:issuer,draweeBank:String(p.draweeBank||'Global Holdings Treasury Bank'),accountId:book(s,t).accounts[0].id,currency:String(p.currency||'USD'),amount:a,note:purpose,purposeCategory:invoiceNumber?'سداد ذمة بالشيك':'دفعة تجارية بالشيك',purposeDetail:purpose,paymentMethod:'شيك مصرفي',beneficiary:payee.name||beneficiary,beneficiaryPartyId:payee.id,issuePlace:String(p.issuePlace||s.profile?.hq||'الرياض، المملكة العربية السعودية'),paymentPlace:String(p.paymentPlace||'المركز المالي الرئيسي'),authorizedSignatory:String(p.authorizedSignatory||s.profile?.founder||'المفوض بالتوقيع'),dueDay,status:'صادر',issuedAt:now(s),invoiceNumber,requestRef:requestRef||null,type:p.type||'شيك مصرفي'};
  s.finance.cheques.unshift(r);if(linked){linked.chequeId=id;linked.paymentMethod='شيك مصرفي';linked.status='شيك صادر';}if(payable){payable.chequeId=id;payable.paymentMethod='شيك مصرفي';payable.status='شيك صادر';}return r;
}
function chequeResult(c,settled,reason){return {id:c.id,settled,status:settled?'settled':'bounced',reason,company:c.company,amount:c.amount,invoiceNumber:c.invoiceNumber||null,requestRef:c.requestRef||null};}
function settleCheque(s,p){const c=s.finance.cheques.find(x=>x.id===p.id);if(!c)throw new Error('cheque-not-found');if(c.status==='مصروف')return chequeResult(c,true,'already-settled');const t=c.company||'group',a=num(c.amount),inv=c.invoiceNumber?s.finance.invoices.find(x=>x.number===c.invoiceNumber):null,payable=c.invoiceNumber?s.finance.payables.find(x=>x.number===c.invoiceNumber):null;if(c.invoiceNumber){if(!inv||!payable)throw new Error('cheque-linked-payable-not-found');if((inv.company||'group')!==t||(payable.company||'group')!==t)throw new Error('cheque-linked-company-mismatch');if(inv.kind!=='مصروف'||['مدفوعة','مسددة','محصلة'].includes(inv.status))throw new Error('cheque-linked-invoice-not-payable');if(Math.abs(num(inv.total??inv.amount)-a)>.01||Math.abs(num(payable.total??payable.amount)-a)>.01)throw new Error('cheque-linked-amount-mismatch');const counterparty=String(inv.counterparty||payable.counterparty||'').trim();if(counterparty&&String(c.beneficiary||'').trim()!==counterparty)throw new Error('cheque-linked-beneficiary-mismatch');}const line=p.line||inv?.budgetLine||lineFor(c.note,'شيك'),reserved=num(inv?.budgetReserved),b=book(s,t);if(!hasFunds(s,t,a)||(!reserved&&!canSpend(s,t,a,line))){c.status='مرتجع';c.returnedAt=now(s);if(inv)inv.status='شيك مرتجع';return chequeResult(c,false,'insufficient-funds-or-budget');}coverInfiniteFunds(s,t,a,'تغطية شيك في وضع اللعب');b.accounts[0].balance-=a;if(reserved)consumeReserved(s,t,a,line);else consumeBudget(s,t,a,line);c.status='مصروف';c.clearedAt=now(s);if(payable){s.finance.payables=s.finance.payables.filter(x=>x.number!==c.invoiceNumber);if(inv){inv.status='مسددة';inv.settledAt=now(s);inv.budgetReserved=0;}journal(s,t,`صرف ${c.id} · تسوية ${c.invoiceNumber}`,[{account:'ذمم دائنة',debit:a},{account:b.accounts[0].id,credit:a}],c.id);}else journal(s,t,`صرف ${c.id} · ${c.note}`,[{account:'مصروف تشغيلي',debit:a},{account:b.accounts[0].id,credit:a}],c.id);const payee=inv?.counterpartyPartyId?{id:inv.counterpartyPartyId,name:inv.counterparty}:partyMeta(s,c.beneficiary,'supplier'),entry={at:now(s),from:b.accounts[0].id,to:payee.name||c.beneficiary,toPartyId:payee.id||c.beneficiaryPartyId||null,amount:a,note:c.note||`صرف ${c.id}`,purposeCategory:c.purposeCategory||'سداد بالشيك',purposeDetail:c.purposeDetail||c.note||'سداد التزام',paymentMethod:'شيك مصرفي',chequeNumber:c.id,label:'تسوية شيك مصرفي',company:t,kind:'cheque-settlement',documentNumber:c.invoiceNumber||null,reference:c.id,status:'منفذة'};ledger(s,t,entry);recordWorldFinance(s,{partyId:entry.toPartyId,counterparty:entry.to,company:t,direction:'outgoing',amount:a,reference:c.id,transferRef:c.id,documentNumber:c.invoiceNumber||null,note:c.note||entry.note,settled:true});reconcile(s);return chequeResult(c,true,'settled');}
function payByCheque(s,p={}){
  const t=TYPES.includes(p.company)?p.company:'group',a=num(p.amount),beneficiary=String(p.beneficiary||p.counterparty||'').trim(),line=p.line||lineFor(p.note,'شيك مصرفي');
  if(a<=0)throw new Error('invalid-cheque-amount');if(!beneficiary)throw new Error('cheque-beneficiary-required');
  const doc=accrue(s,{company:t,amount:a,note:p.note||'شراء من مورد',method:'شيك مصرفي',taxable:p.taxable!==false,counterparty:beneficiary,dueDay:p.dueDay??Math.floor(now(s)/86400),number:p.number,line});
  const cheque=issueCheque(s,{company:t,amount:a,note:p.note||'شراء من مورد',beneficiary,invoiceNumber:doc.number,requestRef:p.requestRef||null,type:p.type||'شيك مصرفي',line,draweeBank:p.draweeBank,issuePlace:p.issuePlace,paymentPlace:p.paymentPlace,authorizedSignatory:p.authorizedSignatory,dueDay:p.dueDay});
  doc.chequeId=cheque.id;doc.paymentMethod='شيك مصرفي';const payable=s.finance.payables.find(row=>row.number===doc.number);if(payable){payable.chequeId=cheque.id;payable.paymentMethod='شيك مصرفي';}
  const settlement=settleCheque(s,{id:cheque.id,line});if(!settlement.settled)throw new Error(`cheque-settlement-failed:${settlement.reason}`);
  return {reference:cheque.id,invoice:doc,cheque,settlement};
}
function founderInvestment(s,p){const t=TYPES.includes(p.company)?p.company:'group',a=num(p.amount);if(a<=0)throw new Error('invalid-amount');const b=book(s,t),founder=s.profile?.founder||'المؤسس',id=p.ref||`FND-${Math.floor(now(s))}-${s.finance.transfers.length+1}`;if(s.finance.transfers.some(x=>x.id===id)||s.treasury.ledger.some(x=>x.reference===id))return {ref:id,amount:0,company:t,idempotent:true};b.accounts[0].balance+=a;const transfer={id,reference:id,at:now(s),kind:'founder-investment',label:'حوالة استثمار واردة',from:`المستثمر · ${founder}`,to:b.accounts[0].id,toAccount:b.accounts[0].id,amount:a,company:t,beneficiaryCompany:t,status:'منفذة',note:p.note||'استثمار المؤسس'};s.finance.transfers.unshift({...transfer,to:companyName(s,t)});ledger(s,t,transfer);journal(s,t,`${id} · استثمار المؤسس`,[{account:b.accounts[0].id,debit:a},{account:'حقوق ملكية المؤسس',credit:a}],id);reconcile(s);return {ref:id,amount:a,company:t};}
function founderWithdrawal(s,p){const t=TYPES.includes(p.company)?p.company:'group',requested=num(p.amount),b=book(s,t),a=Math.min(requested,operating(s,t));if(a<=0)throw new Error('insufficient-cash');const founder=s.profile?.founder||'المؤسس',id=p.ref||`FND-WD-${Math.floor(now(s))}-${s.finance.transfers.length+1}`;if(s.finance.transfers.some(x=>x.id===id)||s.treasury.ledger.some(x=>x.reference===id))return {ref:id,amount:0,company:t,idempotent:true};b.accounts[0].balance-=a;const transfer={id,reference:id,at:now(s),kind:'founder-withdrawal',label:'سحب رأس مال للمؤسس',from:b.accounts[0].id,to:`المستثمر · ${founder}`,amount:a,company:t,status:'منفذة',note:p.note||'تسوية رأسمالية'};s.finance.transfers.unshift({...transfer,from:companyName(s,t),to:founder});ledger(s,t,transfer);journal(s,t,`${id} · سحب رأسمالي للمؤسس`,[{account:'حقوق ملكية المؤسس',debit:a},{account:b.accounts[0].id,credit:a}],id);reconcile(s);return {ref:id,amount:a,company:t};}

function refreshIdentity(s,p){const t=TYPES.includes(p.company)?p.company:'group',name=companyName(s,t);for(const bucket of [s.finance?.invoices,s.finance?.cheques,s.finance?.payables,s.finance?.receivables,s.finance?.periods])for(const doc of (bucket||[])){if((doc.company||'group')===t){doc.company=t;doc.companyName=name;delete doc.logo;delete doc.companyLogo;delete doc.issuerLogo;}}for(const entry of (s.treasury?.ledger||[])){if(entry.company===t)entry.companyName=name;if(entry.beneficiaryCompany===t)entry.beneficiaryName=name;}return name;}
function raiseDebt(s,p){const t=TYPES.includes(p.company)?p.company:'group',a=num(p.amount);if(a<=0)throw new Error('invalid-amount');const b=book(s,t),id=p.ref||`DEBT-${Math.floor(now(s))}-${b.ledger.length+1}`;b.accounts[0].balance+=a;b.debt=num(b.debt)+a;journal(s,t,p.note||'تمويل دين',[{account:b.accounts[0].id,debit:a},{account:p.liabilityAccount||'تسهيلات ائتمانية مستحقة',credit:a}],id);ledger(s,t,{at:now(s),from:p.lender||'جهة تمويل',to:b.accounts[0].id,amount:a,note:p.note||'تمويل دين',company:t,kind:'debt-financing',reference:id});reconcile(s);return {ref:id,amount:a,debt:b.debt};}
function repayDebt(s,p){const t=TYPES.includes(p.company)?p.company:'group',b=book(s,t),a=Math.min(num(p.amount),num(b.debt));if(a<=0)return {amount:0,debt:num(b.debt)};if(operating(s,t)<a)throw new Error('insufficient-cash');b.accounts[0].balance-=a;b.debt=Math.max(0,num(b.debt)-a);journal(s,t,p.note||'سداد أصل دين',[{account:p.liabilityAccount||'تسهيلات ائتمانية مستحقة',debit:a},{account:b.accounts[0].id,credit:a}],p.ref||`DEBT-PAY-${Math.floor(now(s))}`);ledger(s,t,{at:now(s),from:b.accounts[0].id,to:p.lender||'جهة تمويل',amount:a,note:p.note||'سداد أصل دين',company:t,kind:'debt-repayment'});reconcile(s);return {amount:a,debt:b.debt};}
function raiseEquity(s,p){const t=TYPES.includes(p.company)?p.company:'group',a=num(p.amount);if(a<=0)throw new Error('invalid-amount');const b=book(s,t),id=p.ref||`EQUITY-${Math.floor(now(s))}-${s.finance.transfers.length+1}`;b.accounts[0].balance+=a;journal(s,t,p.note||'زيادة رأس مال',[{account:b.accounts[0].id,debit:a},{account:p.equityAccount||'رأس مال وعلاوة إصدار',credit:a}],id);ledger(s,t,{at:now(s),from:p.source||'مستثمرون',to:b.accounts[0].id,amount:a,note:p.note||'زيادة رأس مال',company:t,kind:'equity-financing',reference:id});reconcile(s);return {ref:id,amount:a};}

function zeroSectorMap(){return {air:0,sea:0,road:0,power:0,bank:0,mobility:0};}
function consumeTripAccruals(s){
  ensure(s);
  const result={
    profit:{...zeroSectorMap(),...(s.tripProfitAccrued||{})},
    revenue:{...zeroSectorMap(),...(s.tripRevenueAccrued||{})},
    fuel:{...zeroSectorMap(),...(s.tripFuelAccrued||{})},
    maintenance:{...zeroSectorMap(),...(s.tripMaintenanceAccrued||{})},
    count:{...zeroSectorMap(),...(s.tripCountAccrued||{})},
    cash:{...zeroSectorMap(),...(s.finance.pendingDailyCash||{})}
  };
  s.tripProfitAccrued=zeroSectorMap();s.tripRevenueAccrued=zeroSectorMap();s.tripFuelAccrued=zeroSectorMap();s.tripMaintenanceAccrued=zeroSectorMap();s.tripCountAccrued=zeroSectorMap();s.finance.pendingDailyCash=zeroSectorMap();
  return result;
}
function settleDailyCash(s,p={}){
  ensure(s);const t=TYPES.includes(p.company)&&p.company!=='group'?p.company:null,amount=Number(p.amount),day=Math.max(0,Math.floor(Number(p.day)||Math.floor(now(s)/86400))),reference=String(p.reference||`DAY-CASH-${t||'INVALID'}-${day}`);
  if(!t)throw new Error('invalid-daily-settlement-company');if(!Number.isFinite(amount))throw new Error('invalid-daily-settlement-amount');
  const prior=s.finance.transfers.find(row=>row.reference===reference||row.id===reference);if(prior){if(prior.company!==t||Math.abs(Number(prior.requestedAmount)-Math.abs(amount))>.01)throw new Error('daily-settlement-reference-conflict');return {company:t,amount:0,reference,idempotent:true,status:prior.status};}
  if(p.invoiceNumbers!==undefined&&(!Array.isArray(p.invoiceNumbers)||p.invoiceNumbers.length>20||p.invoiceNumbers.some(number=>!s.finance.invoices.some(row=>row.number===number&&row.company===t))))throw new Error('invalid-settlement-invoices');
  if([p.grossAmount,p.deductions].some(value=>value!==undefined&&(!Number.isFinite(Number(value))||Number(value)<0)))throw new Error('invalid-settlement-breakdown');
  const settledDays=s.finance.dailySettlementDays||{};if(Number.isFinite(settledDays[t])&&day<=settledDays[t])return {company:t,amount:0,reference,idempotent:true,status:'منفذة سابقًا'};
  if(Math.abs(amount)<.005&&!(Number(p.grossAmount)>0))return {company:t,amount:0,reference,idempotent:false,status:'صفر'};
  const b=book(s,t),incoming=amount>=0,requested=Math.abs(amount),settled=incoming?requested:Math.min(requested,num(b.accounts[0].balance)),shortfall=Math.max(0,requested-settled),balanceBefore=num(b.accounts[0].balance);
  if(incoming)b.accounts[0].balance+=settled;else b.accounts[0].balance-=settled;const balanceAfter=num(b.accounts[0].balance);
  if(shortfall>0)s.finance.pendingDailyCash[t]=(Number(s.finance.pendingDailyCash[t])||0)-shortfall;
  const transfer={id:reference,reference,at:now(s),day,from:incoming?'مركز التسوية التشغيلية اليومية':b.accounts[0].id,to:incoming?b.accounts[0].id:'مركز التسوية التشغيلية اليومية',amount:settled,requestedAmount:requested,shortfall,carriedAdjustment:p.grossAmount===undefined?0:amount-(num(p.grossAmount)-num(p.deductions)),signedAmount:incoming?settled:-settled,netAmount:incoming?settled:-settled,note:String(p.note||`تسوية صافي رحلات اليوم ${day} · ${companyName(s,t)}`),purposeCategory:incoming?'وصول صافي التشغيل للحساب الجاري':'تسوية عجز تشغيل',purposeDetail:String(p.note||`صافي الحركة النقدية التشغيلية لليوم ${day}`),paymentMethod:'تسوية تشغيل يومية',company:t,kind:'daily-operating-settlement',label:incoming?'وصول صافي التشغيل للحساب الجاري':'تسوية عجز التشغيل',status:shortfall>0?'منفذة جزئيًا':'منفذة',accountBalanceBefore:balanceBefore,accountBalanceAfter:balanceAfter};
  const profile=collectionProfile(t);Object.assign(transfer,{collection:true,channel:profile.channel,service:profile.service,invoiceNumbers:(p.invoiceNumbers||[]).slice(0,20),grossAmount:num(p.grossAmount??Math.max(0,amount)),deductions:num(p.deductions),tripCount:Math.max(0,Math.floor(Number(p.tripCount)||0)),sourceRefs:[`TRIPS-${t}-${day}`]});if(incoming)transfer.from=profile.source;
  for(const number of transfer.invoiceNumbers){const doc=s.finance.invoices.find(row=>row.number===number&&row.company===t);if(doc)doc.transferReference=reference;}
  s.finance.dailySettlementDays={...settledDays,[t]:day};s.finance.transfers.unshift({...transfer});if(settled>0){ledger(s,t,transfer);journal(s,t,transfer.note,incoming?[{account:b.accounts[0].id,debit:settled},{account:'مركز التسوية التشغيلية اليومية',credit:settled}]:[{account:'مركز التسوية التشغيلية اليومية',debit:settled},{account:b.accounts[0].id,credit:settled}],reference);}reconcile(s);return {company:t,amount:incoming?settled:-settled,reference,idempotent:false,status:transfer.status,shortfall,balanceBefore,balanceAfter};
}
function recordDailyClose(s,p){
  ensure(s);const sectors={...zeroSectorMap(),...(p.sectors||{})},net=Number(p.net)||0,rawDay=Number(p.day),day=Math.max(0,Math.floor(Number.isFinite(rawDay)?rawDay:Math.floor(now(s)/86400))),companies={};
  for(const type of TYPES.filter(t=>t!=='group')){
    const input=p.companies?.[type]||{},grossRevenue=num(input.grossRevenue),expenses=num(input.expenses),companyNet=Number.isFinite(Number(input.net))?Number(input.net):(Number(sectors[type])||0);
    companies[type]={grossRevenue,expenses,net:companyNet,tripRevenue:num(input.tripRevenue),operatingRevenue:num(input.operatingRevenue),tripCount:Math.max(0,Math.floor(Number(input.tripCount)||0))};
  }
  const companyTotals=Object.values(companies).reduce((out,row)=>{out.grossRevenue+=num(row.grossRevenue);out.expenses+=num(row.expenses);out.net+=Number(row.net)||0;out.tripCount+=Math.max(0,Number(row.tripCount)||0);return out;},{grossRevenue:0,expenses:0,net:0,tripCount:0}),groupOverhead=Math.max(0,companyTotals.net-net),report={id:`CLOSE-${day}`,day,at:now(s),net,sectors:{...sectors},companies,group:{grossRevenue:companyTotals.grossRevenue,expenses:companyTotals.expenses+groupOverhead,net,tripCount:companyTotals.tripCount,overhead:groupOverhead}};
  const prior=s.finance.dailyCompanyReports.findIndex(row=>Number(row?.day)===day);if(prior>=0)s.finance.dailyCompanyReports.splice(prior,1);
  s.finance.dailyCompanyReports.unshift(report);s.finance.dailyCompanyReports=s.finance.dailyCompanyReports.slice(0,400);
  s.lastClosedSectorProfit=sectors;s.lastClosedProfit=net;s.sectorProfitToday=zeroSectorMap();s.todayProfit=0;
  return report;
}

function performance(s,t='group',days=30){
  t=TYPES.includes(t)?t:'group';days=Math.max(1,Math.min(365,Math.floor(Number(days)||30)));const currentDay=Math.floor(now(s)/86400),rows=(Array.isArray(s?.finance?.dailyCompanyReports)?s.finance.dailyCompanyReports:[]).filter(row=>Number(row?.day)>currentDay-days&&Number(row?.day)<=currentDay);
  const aggregate=rows.reduce((out,row)=>{if(t==='group'){const x=row.group||{};out.grossRevenue+=num(x.grossRevenue);out.expenses+=num(x.expenses);out.net+=Number(row.net)||0;out.tripCount+=Math.max(0,Number(x.tripCount)||0);}else{const x=row.companies?.[t]||{};out.grossRevenue+=num(x.grossRevenue);out.expenses+=num(x.expenses);out.net+=Number(x.net)||0;out.tripCount+=Math.max(0,Number(x.tripCount)||0);}return out;},{grossRevenue:0,expenses:0,net:0,tripCount:0});
  const latest=rows[0],latestData=t==='group'?{grossRevenue:num(latest?.group?.grossRevenue),expenses:num(latest?.group?.expenses),net:Number(latest?.net)||0,tripCount:Math.max(0,Number(latest?.group?.tripCount)||0)}:{grossRevenue:num(latest?.companies?.[t]?.grossRevenue),expenses:num(latest?.companies?.[t]?.expenses),net:Number(latest?.companies?.[t]?.net)||0,tripCount:Math.max(0,Number(latest?.companies?.[t]?.tripCount)||0)};
  const currentNet=t==='group'?Number(s.todayProfit)||0:Number(s.sectorProfitToday?.[t])||0;
  return {company:t,days,reportedDays:rows.length,currentNet,lastDay:latest?.day??null,lastDayGross:latestData.grossRevenue,lastDayExpenses:latestData.expenses,lastDayNet:latestData.net,lastDayTrips:latestData.tripCount,grossRevenue:aggregate.grossRevenue,expenses:aggregate.expenses,net:aggregate.net,tripCount:aggregate.tripCount};
}

const CALENDAR_EPOCH_SECONDS=Date.UTC(2026,0,1)/1000;
function calendarMonthForDay(day){const date=new Date((CALENDAR_EPOCH_SECONDS+Math.max(0,Number(day)||0)*86400)*1000);return `${date.getUTCFullYear()}-${String(date.getUTCMonth()+1).padStart(2,'0')}`;}
function monthlyStatement(s,{months=12}={}){
  months=Math.max(1,Math.min(36,Math.floor(Number(months)||12)));const byMonth=new Map(),companyTypes=TYPES.filter(type=>type!=='group');
  for(const report of Array.isArray(s?.finance?.dailyCompanyReports)?s.finance.dailyCompanyReports:[]){const key=calendarMonthForDay(report.day),month=byMonth.get(key)||{monthKey:key,days:new Set(),group:{income:0,expenses:0,net:0},companies:{}};month.days.add(Number(report.day));const group=report.group||{};month.group.income+=num(group.grossRevenue);month.group.expenses+=num(group.expenses);month.group.net+=Number(report.net)||0;for(const type of companyTypes){const source=report.companies?.[type]||{},row=month.companies[type]||(month.companies[type]={company:type,income:0,expenses:0,net:0});row.income+=num(source.grossRevenue);row.expenses+=num(source.expenses);row.net+=Number(source.net)||0;}byMonth.set(key,month);}
  const currentKey=calendarMonthForDay(Math.floor(now(s)/86400));if(!byMonth.has(currentKey))byMonth.set(currentKey,{monthKey:currentKey,days:new Set(),group:{income:0,expenses:0,net:0},companies:{}});
  return [...byMonth.values()].sort((a,b)=>b.monthKey.localeCompare(a.monthKey)).slice(0,months).map(month=>({monthKey:month.monthKey,reportedDays:month.days.size,income:month.group.income,expenses:month.group.expenses,net:month.group.net,deficit:Math.max(0,-month.group.net),surplus:Math.max(0,month.group.net),companies:companyTypes.map(type=>month.companies[type]||{company:type,income:0,expenses:0,net:0})}));
}

function applySimulationJournal(s,p={}){
  ensure(s);const j=p.journal&&typeof p.journal==='object'?p.journal:{},types=['air','sea','road','power','bank','mobility'];
  s.todayProfit=(Number(s.todayProfit)||0)+(Number(j.todayProfit)||0);
  s.sectorProfitToday=s.sectorProfitToday&&typeof s.sectorProfitToday==='object'?s.sectorProfitToday:zeroSectorMap();
  for(const k of ['tripProfitAccrued','tripRevenueAccrued','tripFuelAccrued','tripMaintenanceAccrued','tripCountAccrued'])s[k]=s[k]&&typeof s[k]==='object'?s[k]:zeroSectorMap();
  for(const type of types){
    s.sectorProfitToday[type]=(Number(s.sectorProfitToday[type])||0)+(Number(j.sectorProfit?.[type])||0);
    s.tripProfitAccrued[type]=(Number(s.tripProfitAccrued[type])||0)+(Number(j.tripProfit?.[type])||0);
    s.tripRevenueAccrued[type]=(Number(s.tripRevenueAccrued[type])||0)+(Number(j.tripRevenue?.[type])||0);
    s.tripFuelAccrued[type]=(Number(s.tripFuelAccrued[type])||0)+(Number(j.tripFuel?.[type])||0);
    s.tripMaintenanceAccrued[type]=(Number(s.tripMaintenanceAccrued[type])||0)+(Number(j.tripMaintenance?.[type])||0);
    s.tripCountAccrued[type]=(Number(s.tripCountAccrued[type])||0)+(Number(j.tripCount?.[type])||0);
    const cash=Number(j.cash?.[type])||0;if(cash)s.finance.pendingDailyCash[type]=(Number(s.finance.pendingDailyCash[type])||0)+cash;
  }
  reconcile(s);return {todayProfit:Number(s.todayProfit)||0,cash:Number(s.cash)||0};
}
function initializeCapital(s,p){
 const capital=Number(p.capital);if(!Number.isFinite(capital)||capital<=0)throw new Error('invalid-initial-capital');
 if(s.finance?.foundingCapital)throw new Error('capital-already-initialized');
 const ref=String(p.ref||'');if(!ref)throw new Error('founding-capital-reference-required');
 s.companyFinance={};s.companyBudgets={};s.treasury={accounts:[],ledger:[],paymentQueue:[]};
 s.finance={invoices:[],cheques:[],payables:[],receivables:[],periods:[],journalEntries:[],transfers:[],payrollReports:[],paymentSequence:1,invoiceSequence:1,journalSequence:0,taxPayable:0,taxPaid:0};
 ensure(s);s.advanced=s.advanced||{};if(s.advanced.treasury)s.advanced.treasury.liquidityBuffer=0;
 const result=founderInvestment(s,{company:'group',amount:capital,ref,note:'رأس المال عند تأسيس المجموعة'});
 s.finance.foundingCapital={ref,amount:capital};return result;
}
function execute(ctx,cmd,p={}){const s=ctx.state||ctx;ensure(s);switch(cmd){case'initialize-capital':return initializeCapital(s,p);case'ensure':return ensure(s);case'spend':return spend(s,p);case'pay-by-cheque':return payByCheque(s,p);case'pay-payroll':return payPayroll(s,p);case'accrue-payroll':return accruePayroll(s,p);case'record-payroll-report':return recordPayrollReport(s,p);case'accrue-expense':return accrue(s,p);case'credit':return credit(s,p);case'transfer':return transfer(s,p);case'settle-intercompany-interest':return settleIntercompanyInterest(s,p);case'bulk-transfer':return bulkTransfer(s,p);case'transfer-reserve':return transferReserve(s,p);case'collect-receivable':return collectReceivable(s,p);case'settle-payable':return settlePayable(s,p);case'pay-taxes':return payTaxes(s,p);case'issue-cheque':return issueCheque(s,p);case'settle-cheque':return settleCheque(s,p);case'founder-investment':return founderInvestment(s,p);case'founder-withdrawal':return founderWithdrawal(s,p);case'set-budget':{const b=mutableBudget(s,p.company);b.limit=num(p.limit);b.enabled=b.limit>0;if(p.lines)b.lines={...b.lines,...p.lines};if(p.resetSpent){b.spent=0;b.reserved=0;b.spentByLine={};b.reservedByLine={};}return clone(b);}case'reset-budget':{const b=mutableBudget(s,p.company);b.limit=0;b.spent=0;b.reserved=0;b.enabled=false;b.lines={};b.spentByLine={};b.reservedByLine={};return clone(b);}case'refresh-identity':return refreshIdentity(s,p);case'raise-debt':return raiseDebt(s,p);case'raise-equity':return raiseEquity(s,p);case'repay-debt':return repayDebt(s,p);case'set-debt':book(s,p.company).debt=num(p.amount);reconcile(s);return true;case'consume-trip-accruals':return consumeTripAccruals(s);case'settle-daily-cash':return settleDailyCash(s,p);case'apply-simulation-journal':return applySimulationJournal(s,p);case'record-daily-close':return recordDailyClose(s,p);case'close-vat-period':{const day=Math.max(0,Math.floor(Number(p.day)||Math.floor(now(s)/86400))),month=Math.max(1,Math.ceil(day/30)),rows=[];for(const t of TYPES){const b=book(s,t),vat=b.vat||(b.vat={output:0,input:0,creditCarry:0,periodOutputStart:0,periodInputStart:0}),deltaOutput=Math.max(0,num(vat.output)-num(vat.periodOutputStart)),deltaInput=Math.max(0,num(vat.input)-num(vat.periodInputStart)),openingCredit=num(vat.creditCarry),net=deltaOutput-deltaInput-openingCredit,periodTax=Math.max(0,net),closingCredit=Math.max(0,-net);vat.creditCarry=closingCredit;vat.periodOutputStart=num(vat.output);vat.periodInputStart=num(vat.input);const row={id:`TAX-${t}-${day}`,company:t,companyName:companyName(s,t),period:`شهر المحاكاة ${month}`,outputVAT:deltaOutput,inputVAT:deltaInput,openingCredit,closingCredit,amount:periodTax,dueDay:day+15,status:periodTax>0?'مستحق':'صفر'};s.finance.periods.unshift(row);b.taxPayable=periodTax;rows.push(row);}s.finance.taxPayable=TYPES.reduce((n,t)=>n+num(book(s,t).taxPayable),0);s.finance.taxPaid=TYPES.reduce((n,t)=>n+num(book(s,t).taxPaid),0);reconcile(s);return rows;}default:throw new Error(`Unknown finance command: ${cmd}`)}}
const API={VERSION,TYPES,collectionProfile,ensure,makeBook,book,operating,total,budget,remaining,lineRemaining,lineFor,canSpend,consumeBudget,reserveBudget,consumeReserved,releaseReserved,reconcile,journal,invoice,payByCheque,performance,monthlyStatement,calendarMonthForDay,execute};globalThis.GH_FINANCE_CORE=API;globalThis.GH_DOMAIN_COMMANDS?.register?.('finance',API);if(globalThis.window&&window!==globalThis)window.GH_FINANCE_CORE=API;if(typeof module!=='undefined'&&module.exports)module.exports=API;
})();
