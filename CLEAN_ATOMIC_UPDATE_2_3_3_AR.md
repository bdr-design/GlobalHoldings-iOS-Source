# Global Holdings 2.3.3 — Clean Atomic Update Installer

## الهدف
منع تراكم تحديثات WebApp نهائيًا. لا يتم نسخ الإصدار الحالي إلى Staging ولا يتم تثبيت ملف فوق ملف قديم.

## دورة التثبيت
1. التحقق من الحزمة والتوقيع والفهارس.
2. إنشاء `WebApp.staging` فارغ تمامًا.
3. كتابة ملفات الحزمة فقط.
4. فحص الملفات الأساسية ومراجع `index.html`.
5. مقارنة مجموعة الملفات الفعلية في Staging مع Manifest حرفيًا؛ أي ملف مفقود أو يتيم يفشل التحديث.
6. حذف `WebApp.previous` الأقدم فقط، ثم نقل WebApp الحالي ليصبح النسخة السابقة الوحيدة.
7. نقل Staging إلى WebApp بعملية rename داخل Application Support.
8. تسجيل الإصدار كـ Pending Boot.
9. لا يصبح الإصدار مثبتًا نهائيًا حتى يصل `confirmUpdateBoot` بعد اكتمال تحميل cores، ترحيل الحفظ، الخريطة ومحرك المحاكاة.
10. إذا انتهى التطبيق/العملية قبل التأكيد، يستعيد Native النسخة السابقة تلقائيًا قبل تشغيل WebKit في الإطلاق التالي.

## قواعد ممنوعة
- Overlay updates.
- Delta updates.
- `delete[]` في Clean Snapshot.
- بقاء ملف غير موجود في الحزمة الجديدة.
- أكثر من نسخة `WebApp.previous` واحدة.

## الحفظ
Save Vault وLocalStorage خارج مجلد WebApp، ولا يدخلان في عملية تنظيف المحتوى.
