# تقرير فصل محركات Build 340 — مرحلة فصل القراءة والتخطيط والعرض

## ما تغير

حافظت النسخة على الإصدار `3.0.0` وBuild `340` ومخطط الحفظ `2.0.0`. بدأ العمل من مصدر Build 339 النظيف، وبقيت كتابة الحالة وتراجع المعاملة تحت المالك المركزي الحالي.

- **الخريطة:** عاملان مستقلان للاستعلام عن ملكية الأصول وتجميع العرض وحساب نقاط الحركة ومراكز المجموعات. يبقى إنشاء عناصر Leaflet وعرضها في الواجهة.
- **المالية:** عامل تجميعي لتقارير الشهر من لقطة DTO للقراءة فقط؛ القيود والتسويات والكتابة المالية باقية داخل أوامر المالية الحالية.
- **المسارات البرية:** عامل لتخطيط توزيع الشاحنات وطلبات هندسة الطريق. تُراجع النتيجة مجددًا ثم يعتمد إسناد المسار داخل المعاملة المركزية. يتوفر مخطط تعاوني على الخيط الرئيسي عند تعذر بدء العامل، مع إلغاء التخطيط عند إلغاء الطلب.
- **الصور:** طابور فك صور محدود ومتكرر الاستخدام، بثلاث عمليات متزامنة وحد 48 طلبًا منتظرًا وذاكرة LRU محدودة. يبقى عرض `<img>` غير المتزامن مسار التوافق.

كل عامل يستقبل مدخلات مستقلة ولا يقرأ حالة اللعبة الحية ولا يكتبها. لم يتغير مخطط الحفظ، وبقي Full Snapshot فعالًا ولم يُفعّل Field Journal.

## الاختبارات المحلية

- فلترة ملكية الخريطة: 4,300 و20,000 صف، ترتيب ثابت، نتائج قديمة ومسار احتياط: ناجح.
- عرض الخريطة: مطابقة انتقاء الأصول والمسارات ومراكز التجميع عند 4,300 و20,000: ناجح.
- تقرير المالية: مطابقة دقيقة على 10,000 سجل يومي، بما فيها صافي الرصيد وعد الأيام المكررة: ناجح.
- تخطيط الطرق: السلوك الحالي التعاوني ومطابقة العامل على 4,300 و20,000 وإلغاء الطلب: ناجح.
- محرك الصور: حد التزامن والطابور، الأولوية، إزالة التكرار، وحد الذاكرة: ناجح.
- اختبارات كتابة المحاكاة والتراجع الكامل السابقة وفحوص JavaScript النحوية: ناجحة محليًا.

هذه اختبارات Node على بيانات اصطناعية؛ لا تثبت معدل الإطارات على هاتف. قبل التحويل إلى الشجرة المباشرة، نجح CI رقم `36228909590` على commit `80f697d36e8166cc9eecf69524d106991d6e8443` في بناء المحتوى المعاد تركيبه؛ لكنه لا يثبت تتبع المصدر مباشرة. بعد التحويل، اجتاز CI رقم `36232174407` على commit `bd6bbc25fe18529fe26c2723eeafe39f5e5b9463` بوابات المصدر 109/109 وESLint وChromium/WebKit واختبارات التخزين وWKWebView وبناء تطبيق Release غير موقع لـarm64. لم يُنشئ هذا التشغيل IPA ولم يختبر iPhone فعليًا.

## ما زال داخل المالك المركزي

لم تُنقل بعد معالجة تقدم الأصول داخل المحاكاة، إقفالات اليوم والساعة المالية، تجهيز ونشر سجلات الشراء، بيع الأصل، التحقق النهائي من الأوامر، التراجع الذري، إنشاء علامات Leaflet، أو إدراج الحفظ الأصلي إلى عمال منفصلة. هذه المسارات تتداخل مع كتابات الحالة والمعاملات والهوية والتخزين، ويجب نقل حساباتها كخطط غير قابلة للكتابة ثم اعتمادها ذريًا من المالك المركزي. لذلك لا أصف فصل كل أعباء اللعبة بأنه مكتمل بعد.

بوابة الإصدار والإنتاج باقية مغلقة حتى اختبار iPhone فعلي عند 4,300 و20,000 أصل مع المسارات والشراء والبيع والحفظ والاستعادة وقياسات 90Hz/60Hz واللمس. أظهر اختبار الضغط المحلي على Node عند 5,000 أصل نسخ حالة 99.1ms وJSON.stringify ‏59.53ms وتسليم دفعة 141ms؛ لذلك لا أعد هذه المرحلة حلًا كاملًا للاق على الجهاز.


## مصدر Git المباشر

الـworkflow يتحقق الآن من ملفات المصدر الموجودة في جذر الفرع مباشرة. بصمة شجرة التشغيل: `8db8634d642ed777808160222ab5bac2efad07c75116fa4f49adb6a0c9ed3974`. بقيت بوابة الإصدار مغلقة حتى اختبار الضغط واللمس والإطارات على iPhone فعلي عند 4,300 و20,000 أصل.
