# Global Holdings 3.0.0 - BUILD300

## نطاق الإصدار

- توحيد هوية الإصدار على `3.0.0` ورقم البناء على `300` في WebApp وNative وبيانات المشروع ومولد التحديث.
- إبقاء Save Schema على `2.0.0` دون أي تغيير أو ترحيل قسري.
- جعل `VERSION` و`BUILD` في جذر المصدر مرجع مولد حزمة التحديث، لمنع رجوع تعارض الأرقام.
- إصدار حزمة `full-web` بنمط `clean-snapshot-v1` فقط، مع توقيع Ed25519 وفشل كامل عند غياب المفتاح.
- إصلاح احتواء اسم المجموعة في شريط iPhone العمودي ومنع خروجه خارج حدود الشاشة.
- إزالة المسار البديل الصامت من اختبار فتح GH Mobility؛ يجب أن ينجح التحكم المرئي نفسه.

## بوابات الإصدار

1. نجاح اختبارات المصدر والحراسة كاملة.
2. نجاح Chromium وWebKit دون fallback يخفي فشل الواجهة.
3. مطابقة ملفات WebApp داخل التطبيق والحزمة بالمصدر حرفيًا وبالبصمات.
4. إثبات أن `3.0.0` أحدث من خط الأساس المثبت `2.9.1` وأن إعادة التثبيت أو التخفيض مرفوضان.
5. اختبار تثبيت وترقية واسترجاع على IPA فعلي قبل اعتماده للإنتاج.

## عقود غير قابلة للتغيير

- Simulation Core هو المالك الوحيد للوقت.
- كل تغيير حالة أو عملية مالية ذري All-or-Nothing مع rollback.
- لا Overlay ولا تنفيذ عمليات خارج Transaction.
- أي خطأ في التحقق أو التوقيع أو البصمات يفشل التحديث كاملًا.
- المفتاح الخاص لا يدخل المستودع أو حزمة التسليم.
