# تقرير إصلاح التزام المصروف المستحق — Build 339

**الحالة:** مرشح مصدر مُصحّح، وليس IPA.
**الإصدار:** 3.0.0 · **Build:** 339 · **Save Schema:** 2.0.0
**Runtime SHA-256:** `f47eca7a668f2c9b2ea3d41d1463033c11573e4530cc751a39dbd4cba9ec38b5`
**WebApp SHA-256:** `eaa9b81fd29d9df4533c31d9cfb564e993f4eb58f1cb7af2b33fc0e5ea56dfda`

## العطل الذي عولج

توقف التقدم الزمني عند `simSeconds=949800` أثناء الانتقال إلى `950400`. فشل `postAccruedExpense` في `finance-core.js` برسالة `insufficient-budget` عند تسجيل التزام تشغيلي متحقق لم يتوفر له نقد كافٍ. هذا يمنع تسجيل الذمة، مع أن عدم الدفع يجب أن يبقيها مستحقة.

## التغيير

- تسجيل كامل الالتزام المستحق دون اختراع نقد أو خصم رصيد الحساب.
- حجز الجزء المتاح فقط من ميزانية الشركة وبندها، وتوثيق الباقي في `budgetUnbudgetedAmount`.
- بقاء المبلغ المحجوز حاجزًا أمام المصروفات الاختيارية الجديدة.
- إبقاء التسوية مشروطة بتوفر النقد؛ فشل الدفع لا يغيّر الحالة، ويستهلك التسديد لاحقًا الجزء المحجوز فقط.
- الحفاظ على سقف الميزانية، وSave Schema 2.0.0، ومسار التراجع الكامل. لم يتغير `simulation-core.js` أو `transaction-core.js`.

## التحقق

- `python3 tools/verify_current_source.py`: PASS، 482 ملف runtime.
- `npm run test:build339`: PASS، بما في ذلك تتبع الإطارات 28/28 واختبارا الالتزام المستحق 2/2.
- اختبارات توافق مالية سابقة: PASS (`core-regressions`, Build 317/334/338).
- قبول المصدر الكامل: غير مكتمل. أول اختبارين سجلا PASS، وفشل اختبار المتصفح عند تشغيل Playwright لغياب Chromium؛ أُوقف التشغيل قبل بقية البوابات. لا يوجد ادعاء بقبول كامل.
- بناء Apple/iPhoneOS وقياس iPhone/WKWebView الفعلي: لم يُنفذا. لا يتوفر `xcodebuild` في بيئة العمل.

## حدود النتيجة

يعالج هذا التغيير توقف المحاكاة بسبب الذمة غير الممولة. لا يثبت تحسن FPS. التشخيص السابق سجّل `maxFinishMs≈419` ووقت حفظ متزامن يقارب `197ms`، ولم تُقَس آثار التسجيل على iPhone فعلي؛ لذلك تبقى مشكلة هبوط الإطارات بحاجة إلى اختبار الجهاز وتتبع الأداء قبل إعلان إصلاحها.

**الحالة النهائية:** `ipa_built=false` · `device_tested=false` · `production_approved=false`. بوابة الإصدار ما زالت مغلقة.
