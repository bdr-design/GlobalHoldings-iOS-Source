# Global Holdings — BUILD251 — حالة التسليم الحالية

الإصدار 2.9.0، البناء 251، ومخطط الحفظ 2.0.0.

هذه حزمة المستودع الكاملة، وليست IPA. تتضمن الشيفرة والاختبارات وملف قفل الاعتماديات وCI المحدث.

## ما تم إثباته حتى هذا التسليم
- اجتازت ملفات الاختبار غير المعتمدة على المتصفح: 95/95.
- اجتازت حواجز ESLint وملكية المجالات وملفات Runtime وعقود Native الساكنة.
- اجتازت دورة Chromium الكاملة: تأسيس وتمويل وقاعدة وطلب ثلاثة أصول واعتماد ودفع وتسليم وتوظيف ومسار ومغادرة وإيراد ومصروف وحفظ وإعادة تحميل ومقارنة الأرصدة وإعادة ضبط.
- اجتازت اختبارات المتصفح للحفظ التالف والمستقبلي وانقطاع CDN وفشل سعة التخزين ورفض Native القديم.
- اجتاز التدقيق الحالي لاعتماديات npm بعد رفع Playwright إلى 1.55.1: لا ثغرات معروفة مبلّغ عنها في هذا التدقيق.

أُعيد التحقق بتاريخ 2026-09-07 بعد npm ci: نجاح 95/95، ونجاح التنقل وحالات الفشل الخمس والدورة الكاملة على Playwright 1.55.1 وChromium 140.0.7339.186. عند نهاية التشغيل قبل إعادة الضبط: critical=0 وwarning=0، وثلاثة أصول وثلاثة تسليمات ومسار واحد ودفتر مالي متوازن. التدقيق npm audit أبلغ عن صفر ثغرات معروفة.

## إصلاحات الجذر
عقود الشيكات والدفع؛ حفظ بحدود حجم وقراءة تحقق؛ ACK مترابط بالطلب والبصمة والجيل؛ إعادة ضبط مع تعويض؛ رفض المخططات غير المدعومة؛ ترحيل دون حذف الأصل قبل التحقق؛ توظيف عبر HR فقط؛ ملكية الاستحواذ عبر Corporate؛ تأسيس مالي واحد؛ معاملات مشتركة وتراجع واحد؛ سجل تكرار محدود؛ قائمة Runtime مشتركة؛ حماية الشبكة وCSP/SRI؛ ومنع تكرار احتساب رصيد الشركات التابعة عند إعادة التحميل.

أصلحت المراجعة أيضًا تعطل نسخ الحقول الاختيارية للأصول الجديدة، واتجاه المغادرة الأولى، وفحص الطاقم في المغادرة الآلية، وتحذير الطاقة عندما لا توجد شركة طاقة نشطة.

## الفحوص المتبقية خارج البيئة الحالية

- WebKit لا يعمل محليًا بسبب مكتبات نظام مفقودة؛ أضيف تشغيله إلى CI على macOS.
- Swift parse وXcode Release iphoneos الفعليان غير منفذين هنا لعدم توفر Xcode.
- التحقق الفعلي من موارد .app وIPA ينتظر البناء. حارس الملفات والبصمات نفسه اختُبر بحقن missing/extra/hash/duplicate.
- اختبار iPhone حي: تعليق/استئناف وإغلاق قسري وتحديث/تراجع وتصدير تقارير الدعم.

لا تُعد هذه الحزمة Production Baseline معتمدة قبل اجتياز بوابات Xcode وIPA والجهاز. يلزم إعادة بناء تطبيق iOS ببناء 251؛ استبدال WebApp وحده فوق Native أقدم غير مدعوم.

## إعادة التحقق
```sh
npm ci
npm test
npx playwright install chromium
npm run test:browser
npm run test:e2e
node tests/browser-failures.js
```

قائمة SHA-256 تشمل كل ملف مصدر داخل الحزمة باستثناء ملف البصمات نفسه. مجلد evidence يحتوي نتائج الاختبارات المتاحة عند هذا التسليم. تستكمل المراجعة من هذا المستودع.
