# BUILD307 — إصلاح جذري للسرعة والمنشآت والمسارات

## حدود السلامة

- لم يتغير `simulation-core.js`: بقي مالك `simSeconds` الوحيد، وبقيت الشرائح الذرية وحدود الساعة واليوم و`minRealSliceSeconds = 0.5` كما هي.
- بقي Save Schema `2.0.0`. الحقول الجديدة اختيارية وتُهيأ عند قراءة حفظ أقدم.
- التنعيم طبقة عرض فقط: تقرأ آخر حالة ملتزمة وتحرّك علامة الخريطة بين هدفين عبر `requestAnimationFrame`، ولا تكتب في الوقت أو التقدم أو المالية.

## السرعات والحركة

- خمس سرعات تشغيل فعلية: `30×` و`60×` و`120×` و`300×` و`600×`، إضافة إلى الإيقاف.
- المستويات المحفوظة القديمة `1 / 2 / 4` تحتفظ حرفيًا بمعدلاتها `30 / 120 / 600`، لذلك لا يتغير معنى ملفات الحفظ القائمة.
- مدة الاستيفاء المرئي تتكيف مع الفاصل المقاس بين أهداف المحاكاة وتغطيه كاملًا؛ لا توجد دفعة قصيرة يعقبها سكون بين التحديثات.
- السرعة المرئية منفصلة عن سرعة اللعبة ومحدودة حسب المسافة الظاهرة على الشاشة؛ عند `600×` سقفها `72px/s` حتى لا تعبر العلامة الخريطة بسرعة مزعجة، بينما يبقى الوصول المنطقي والإيراد في موعدهما الأصلي.
- حاكم الأداء والحارس القاتل وخفض السرعة الوقائي ما زالت داخل Simulation Core بلا مسار بديل.

## فتح المنشآت

- `facility-core.js` هو الحارس النهائي لمنشآت اللوجستيات والطاقة والبنك وMobility، وليس زر الواجهة.
- الموقع يجب أن يحمل مفتاحًا مطابقًا `site:<company>:<capitalId>`، وعاصمةً ومدينةً ودولةً وإحداثيات مطابقة للسجل الأصلي.
- يُرفض الموقع المصطنع، ونوع الشركة المخالف، والموقع المكرر، وفتح منشأة لشركة غير مؤسسة حتى عند الاستدعاء الداخلي المباشر.
- قراءة البنك لا تصنع فروعًا ضمنيًا. ترحيل الفروع القديمة معزول، أما الفتح الجديد فيتطلب منشأة بنكية موثقة، ويُنشأ الفرع والمنشأة في معاملة واحدة قابلة للتراجع بالكامل.

## ملكية المسارات

- لكل أصل طيران أو بحر أو طريق مسار واحد حصري. يرفض Fleet Core مشاركة معرف المسار أو هندسته الكاملة حتى لو اختلف المعرف.
- الإرسال الدولي يحجز الوجهات والهندسات أثناء الدفعة نفسها، ويولّد مسارًا مختلفًا لكل أصل. الأصل الذي لا يجد مسارًا مستقلًا آمنًا يبقى متوقفًا بدل مشاركته مسار أصل آخر.
- عند وجود تكرار قديم يُحتفظ بتكليف واحد فقط؛ التكرارات غير المتحركة تُفك فورًا، والمتحركة تكمل رحلتها الحالية بأمان ثم تحرر المسار وتتوقف. لا يحدث نقل آني ولا إيراد وهمي.
- في Mobility لا يبدأ طلب ثانٍ يحمل زوج الشوارع النشط نفسه؛ يبقى في الطابور حتى يتحرر المسار.

## بوابات الانحدار

- مقارنة حتمية تثبت أن معدلات `30× / 120× / 600×` تنتج النتيجة نفسها قبل إضافة المستويين الوسيطين.
- اختبارات تمنع طبقة العرض من تعديل `simSeconds` أو تقدم الأصل.
- اختبارات مباشرة للمواقع المزورة، تكرار المنشأة، غياب منشأة البنك، والتراجع الذري بعد فشل مصطنع.
- اختبارات تمنع مشاركة معرف المسار والهندسة، وتسمح بهندستين مختلفتين فعلًا، وتمنع تراكب مسارات Mobility النشطة.
- Browser QA يقيس الحركة إطارًا بإطار عند `600×` ويتحقق من وجود عينات متتابعة بلا قفزة مهيمنة.
